// FlashAttention interactive report — chart loader
document.addEventListener('DOMContentLoaded', function() {
    console.log('FlashAttention report loaded');
    // Plotly charts will be rendered once benchmark data is integrated
});
